package pij.day06solutions.phones;

public class RestrictedSmartPhone extends SmartPhone {

    // Ex 5:
    // The below commented-out code will *not* be accepted by the compiler.
    // The reason is that the method has been declared in a superclass
    // with public visibility. We cannot reduce its visibility to private!

    /*
    @Override
    private void playGame(String game) {
        System.out.println("No can do!");
    }
     */
}
